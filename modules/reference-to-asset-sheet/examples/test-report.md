# 非鸟类示例测试

测试日期：2026-09-16

## 测试范围

只验证：参考图理解、元素清单、分板、提示词编译和 Sheet 生成。不执行去背景或切割。

## 结构测试

- 示例识别为 8 个视觉原型，分成 2 张 Sheet。
- 提示词编译器通过 3 项测试：正常编译、拒绝重复分配、拒绝超出格子容量。
- 每个资产恰好分配一次，提示词保留资产 ID、顺序、画风和背景要求。

## 实际生成结果

### Sheet 1

- 六个指定元素全部出现，顺序正确。
- `THANK YOU` 拼写正确。
- 元素没有串格或截断。
- 模型把棋盘格画进了不透明图片；文件 Alpha 全为 255。

### Sheet 2

- 圆环和虚线轨迹均出现，第三格保持空白。
- 模型仍把棋盘格画进了不透明图片；文件 Alpha 全为 255。

## 结论

清单、分板、提示词编译和 Sheet 语义生成已在非鸟类参考图上跑通。透明背景指令不可靠，但透明检测与去底不属于本模块；生成结果应原样交给后续模块。当前成熟度保持 `candidate`，待更多不同画风和元素密度的参考图验证后再升级。
